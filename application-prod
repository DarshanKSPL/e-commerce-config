spring:
  application:
    name: Config-Prod
  eureka:
    instance:
      prefer-ip-address: true
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka

logging:
  level:
    org.springframework: INFO
    feign: DEBUG

jwt:
  secret: mysecuresecretkeymysecuresecretkey  # at least 256-bit (32 characters)
  expiration: 3600000 # 1 hour in milliseconds
